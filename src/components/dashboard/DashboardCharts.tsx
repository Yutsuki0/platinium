"use client";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis, PieChart, Pie, Cell } from "recharts";
export function DashboardCharts({monthly=[],distribution=[]}:{monthly?:{month:string;count:number}[];distribution?:{name:string;value:number}[]}){const palette=["#64748b","#2cff79","#63ff9b","#f59e0b","#34d399"];return <div className="grid gap-6 xl:grid-cols-2"><section className="glass-panel p-5"><h2 className="font-display text-lg font-semibold text-white">Succès débloqués par mois</h2><p className="mt-1 text-xs text-slate-500">Les 12 derniers mois ayant une activité enregistrée.</p><div className="mt-5 h-72">{monthly.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={monthly}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.06)"/><XAxis dataKey="month" tick={{fill:"#64748b",fontSize:11}} axisLine={false} tickLine={false}/><YAxis allowDecimals={false} tick={{fill:"#64748b",fontSize:11}} axisLine={false} tickLine={false}/><Tooltip contentStyle={{background:"#06130b",border:"1px solid rgba(255,255,255,.1)",borderRadius:12,color:"white"}}/><Bar dataKey="count" fill="#35ff7a" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer>:<Empty/>}</div></section><section className="glass-panel p-5"><h2 className="font-display text-lg font-semibold text-white">Répartition des progressions</h2><p className="mt-1 text-xs text-slate-500">Nombre de jeux par tranche de complétion.</p><div className="mt-5 h-72">{distribution.some(d=>d.value)?<ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={distribution} dataKey="value" nameKey="name" innerRadius={55} outerRadius={90} paddingAngle={3}>{distribution.map((_,i)=><Cell key={i} fill={palette[i%palette.length]}/>)}</Pie><Tooltip contentStyle={{background:"#06130b",border:"1px solid rgba(255,255,255,.1)",borderRadius:12,color:"white"}}/></PieChart></ResponsiveContainer>:<Empty/>}</div><div className="flex flex-wrap justify-center gap-3">{distribution.map((d,i)=><span key={d.name} className="text-xs text-slate-400"><i className="mr-1.5 inline-block h-2 w-2 rounded-full" style={{background:palette[i%palette.length]}}/>{d.name}: {d.value}</span>)}</div></section></div>}
function Empty(){return <div className="flex h-full items-center justify-center text-sm text-slate-600">Pas encore assez de données.</div>}
